var prototypeConstructor = (function() {
    /*
    
    Question 2.

    This time, we'll use JavaScript's constructor functions to create our prototype chain.

    (a) Start by creating the base class Animal.
        (You will need to expose it publicly as it's used in the tests.)
    (b) Create the derived class Cat and ensure that it's prototype chain includes Animal.

     */
    return {};
})();